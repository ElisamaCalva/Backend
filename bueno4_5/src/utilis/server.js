//import ratelimit from 'express-rate-limit'

/*const ratelimit = require ('express-rate-limit')

function notFound(req, res, next){
    res.status(404)
}

const limitarUsuarios = ratelimit ({
    windowMs: 1000,
    max: 1,
    message: "Has excedido la cantidad de peticiones, vuelve intentarlo mas tarde"
})


const express = require('express')
const app = express()
app.use(limitarUsuarios)
const port = 3000

app.get('/', (req, res) => {
  res.send('Ganbare Senpai')
})

app.listen(port, () => {
  console.log(`Example app listening on port ${port}`)
})

*/
 